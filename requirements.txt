# --- Core framework -------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database / ORM --------------------------------------------------------
sqlalchemy==2.0.35
alembic==1.13.2

# --- Validation / settings --------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Security ----------------------------------------------------------------
# NOTE: passlib 1.7.4 (its latest release) is incompatible with bcrypt>=4.1
# (it reads a `__about__.__version__` attribute that newer bcrypt removed),
# so bcrypt is pinned to the last known-compatible 4.0.x release.
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
python-multipart==0.0.9

# --- Testing -----------------------------------------------------------------
pytest==8.3.3
pytest-cov==5.0.0
httpx==0.27.2

# --- Utilities ----------------------------------------------------------------
python-dotenv==1.0.1
